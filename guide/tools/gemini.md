# Gemini Notes

Gemini can use `GEMINI.md` as a thin entrypoint that references `AI_AGENT_GUIDE.md` and `AI_AGENT_PROJECT.md`.

Gemini-specific instructions should stay short: follow the canonical guide, include touched files in summaries, and report verification commands. Do not duplicate the shared guide in `GEMINI.md`.
